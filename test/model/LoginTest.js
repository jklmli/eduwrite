var login = require("../../node/model/Login.js");


describe("login", function () {
  it("should fail if email does not exist");

  it("should fail if password is incorrect");

  it("should succeed if email, password match an existing account");
});
